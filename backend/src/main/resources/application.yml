spring:
  application:
    name: goodfunds
  profiles:
    active: dev
  jpa:
    open-in-view: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

jwt:
  secret: ${JWT_SECRET:dev-secret-please-change-in-production}
  expiration: 86400000
