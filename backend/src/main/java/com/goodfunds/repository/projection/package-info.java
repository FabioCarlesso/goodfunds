/**
 * Projecoes de consulta (constructor expressions) dos repositorios Spring Data JPA.
 *
 * <p>Tipos imutaveis resultantes de agregacoes/consultas das interfaces em
 * {@code com.goodfunds.repository}. Mantidos fora do pacote de DTOs de API
 * ({@code com.goodfunds.dto}, reservado a contratos REST) por serem detalhe da
 * camada de acesso a dados.</p>
 */
package com.goodfunds.repository.projection;
