# Copie para `.env.local` antes de rodar `npm run dev`.
# Apenas variaveis prefixadas com VITE_ ficam disponiveis no frontend.

# URL base da API REST do backend Goodfunds.
VITE_API_BASE_URL=http://localhost:8080
